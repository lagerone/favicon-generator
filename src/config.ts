import path from 'path';

export const ROOT_PATH = path.join(__dirname, '../');

export const PUBLIC_DIR_PATH = path.join(__dirname, 'public');

export const DEFAULT_ICON_SIZE = 144;

export const ICON_SIZES: number[] = [
  16,
  32,
  48,
  80,
  96,
  128,
  144,
  152,
  180,
  192,
  384,
];
